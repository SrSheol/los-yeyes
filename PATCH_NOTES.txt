v25.1 — Fix blank page / removeChild on load (2026-09-21)

- Root cause: fleet chooser filled a <p data-k23-rec> with <button>/<span>; browsers hoist
  <button> out of <p>, then Design/React removeChild crashes and the page stays blank.
- Changed host to <div class="k23-chooser-out">.
- MutationObserver no longer watches childList on body (was racing React mount); only
  re-binds on data-lang after settle. First refresh waits for .lm to exist.

KARÚN v25 — fix React removeChild crash (DOM mutations) — 2026-09-21

- Boarding-pass sample names: 3 static spans in markup; cycle via class/hidden/aria-hidden only (no innerHTML).
- MutationObserver disconnects for the duration of refresh(); ignores mutations under JS-owned hosts
  ([data-k24-signname], [data-k23-rec], zone chips/nodes/list, .k23-zp-hotels, stats/dest/svc leaves).
- Cache bump: karun-v23.css / karun-v23.js ?v=25.

KARÚN v24 — pulido dirigido sobre v23 — 2026-09-21

1) Selector de idioma (rediseño)
   - Botón tipo píldora con "orbe" de vidrio + código + chevron; estado claro en hero, arena en la isla.
   - Menú: isla de vidrio teal anclada bajo el botón (con flecha), saludo caligráfico "Hola · Hello ·
     Bonjour", 10 idiomas en 2 columnas con código en sello redondo, idioma activo en arena con ✓.
     role=menu / menuitemradio + aria-checked; foco al idioma activo al abrir; flechas, Home/End,
     Escape (cierra y devuelve el foco). En teléfono se mantiene dentro de la pantalla.
   - Drawer móvil: tira "Idioma" con los 10 códigos (44 px) — cambiar idioma sin salir del menú.
   - Español sigue por defecto; misma lógica de estado (langs / lg.pick) — sin cambios en la i18n.

2) Pase de abordar del hero
   - El letrero muestra nombres de ejemplo creíbles (Antonio Camino → Familia Rodríguez → Sarah & Tom
     Miller, relevo suave cada ~4 s; estático con prefers-reduced-motion) y "KARÚN · BIENVENIDOS".
   - Caption caligráfico con flecha: "Así te espera tu letrero en llegadas" / "Your sign, waiting for you
     at arrivals". El letrero real (canvas en #reservar) no cambia.

3) Navbar
   - Fuera del hero la isla queda fija arriba siempre (se eliminó data-k23-nav-hide). Sobre el hero sigue
     transparente. Aplica igual en móvil.

4) Flota
   - Sin sello de recomendado en las pestañas (se quitó el badge data-rec).
   - "¿Cuál me conviene?" ahora responde en tono de ficha: "Para 2 pasajeros · Hyundai Grand Starex ·
     clase 1–6 pax · desde US$20 por vía · PUJ" + "Ver ficha →" (botón contorno, no CTA de venta).

5) Zonas (rediseño "El corredor del Este")
   - Barra superior: toggle PUJ/SDQ tipo etiqueta de equipaje + 3 cifras vivas del tarifario
     (zonas · hoteles · desde).
   - Mapa: mar con profundidad y olas animadas, halo de espuma y orilla punteada, tierra con relieve
     (sombra) y duna, rosa de los vientos, etiquetas en Gloock, nodos con el conteo real dentro,
     aeropuertos como etiquetas, ruta coral y un punto "van" que recorre el corredor del aeropuerto
     elegido (solo en pantalla; apagado con reduced-motion). En móvil PUJ se encuadra la costa este.
   - Panel en vidrio como el pase del hero: nombre, conteo, "desde" en acento caligráfico, hoteles con
     filas punteadas y botón arena que filtra el tarifario. Chips de zona en tira deslizable, tiempos de
     viaje en tarjetas. Olas de orilla al pie de la sección. Datos 100 % del tarifario.

6) Móvil (≈390 px)
   - Nav del hero: márgenes y safe-area, botones de 44 px alineados, sin barra de progreso, marca visible
     también con el drawer abierto (antes quedaba tapada) y el botón de idioma se oculta ahí.
   - Cierre/footer: CTAs a ancho completo, boleto centrado, orden Marca → Contacto → Explora (rejilla de
     2 columnas con blancos táctiles) → Legal → wordmark → "Volver arriba" como botón → créditos apilados.

7) Legal
   - Footer: tarjeta "Legal" con 4 accesos descriptivos (Privacidad · Términos · Cancelaciones y
     reclamos · Cookies y preferencias) en lugar de 6 enlaces sueltos (los duplicados abrían el mismo
     documento).
   - Documentos: cabecera teal con ola, título en Gloock, línea "versión demo · septiembre 2026",
     secciones numeradas, barra "Otros documentos" para saltar entre ellos; en móvil se abren como hoja
     inferior con scroll propio.
   - Términos: se corrigió "hasta 8 pasajeros con 8+ maletas" → capacidades reales por clase.

Sin cambios: tarifario (JSON/.data.js), karun-wizard.js, support.js, image-slot.js, tools/, stack y modal
de Destinos. Sin banner demo arriba, sin sol, sin reseñas/cifras inventadas.

Pruebas (Chromium headless, 1440×900 y 390×844): idioma por menú (teclado incl.) y por drawer; nav fija
tras el hero al bajar; wizard 1→3, confirmación, letrero, editar; cotizar desde flota y tarifario; mapa
SDQ → Santo Domingo (14 hoteles, desde US$60) → tarifario filtrado; modal de Destinos + CTA; documentos
legales y cambio entre ellos; ES/EN/DE sin textos cruzados; sin scroll horizontal; 0 errores de consola;
0 animaciones infinitas con reduced-motion.

---
KARÚN v23 — "otra liga" (rediseño completo sobre v22) — 2026-09-21 (America/Mexico_City)

0) Pedidos explícitos del cliente
   - Eliminada la barra superior "Demo pública…". La honestidad de demo vive solo dentro de #reservar
     (nota con chip DEMO + textos en pago y confirmación).
   - Eliminado el sol del hero (markup .hero-sun, CSS en index/karun-beach.css y assets/karun-fx.js).
   - Sin prueba social falsa: fuera ★4.9, "12,000+ traslados", "30+ países", "2,300+ viajes" (claves i18n
     borradas en los 10 idiomas) y las 4 reseñas inventadas. Las cifras visibles salen del tarifario real
     (249 hoteles, 23 zonas, 4 clases) y se calculan en vivo desde KRN_TARIFARIO.

1) Arquitectura de información (embudo nuevo)
   Hero → Tu llegada (#llegada) → Servicios → Flota → Tarifas → Reservar → Zonas → Destinos →
   Promesas (#promesas) → FAQ → Cierre/Footer.
   - #book (formulario rápido de WhatsApp) eliminado: una sola ruta de reserva (#reservar). WhatsApp queda
     como vía secundaria (bookWhats ahora abre un mensaje-plantilla corto).
   - "Por qué Karún", banda editorial, franja de marquesina y Reseñas → absorbidos en "Lo que sí podemos
     prometerte" (condiciones reales de los términos: precio publicado, cancelación 24 h, espera 60–90 min,
     formas de pago, vehículo privado, atención humana) + banda de foto con frase de marca.
   - "Antes de aterrizar" sube como historia de llegada, justo después del hero.

2) Tipografía
   - Gloock (display, OFL) + Yellowtail (acento caligráfico de playa, Apache 2.0) + Figtree (texto, OFL).
     Self-hosted woff2 + licencias en assets/fonts/. Fraunces retirada. Tildes, ñ, ¿ ¡ verificadas.
   - 1001fonts sirvió de referencia de estilo; no se usaron sus archivos (licencias comerciales dispares).

3) Secciones
   - Nav: transparente sobre el hero → isla de vidrio arena al salir; subrayado de sección activa, marea de
     progreso, se esconde al bajar rápido (escritorio). Menú de idioma en hoja de vidrio. Móvil: drawer
     tipo isla a pantalla casi completa, numerado, con CTA y WhatsApp.
   - Hero: video + gradación teal, luz cáustica, espuma en canvas, palmeras con balanceo, titular palabra a
     palabra, tarjeta "pase de abordar" con letrero, 3 CTA (reservar / tarifa de mi hotel / WhatsApp),
     póster de respaldo con foto de Bávaro.
   - Tu llegada: recorrido scroll-scrubbed avión → van → lobby sobre una ruta SVG; 5 pasos que se encienden;
     tarjeta "¿Vuelo retrasado? Te esperamos". Móvil: línea vertical que se llena.
   - Servicios: 9 capítulos horizontales guiados por el scroll vertical (sticky, sin secuestrar la rueda):
     recibimiento, seguimiento de vuelo, espera incluida, madrugada 24/7, equipaje, sillas, idiomas,
     multi-tramo, hotel a hotel/excursiones. Móvil y reduced-motion: tarjetas apiladas.
   - Flota: showroom (pestañas + escenario foto/ficha: "ideal para", asientos animados, "elígela si…",
     "a bordo", desde US$ PUJ/SDQ, cotizar) + "¿Cuál me conviene?" (pasajeros + prioridad → recomienda
     unidad y muestra el mínimo real de la clase desde PUJ) + barra de capacidad por clase.
   - Tarifas: mismos IDs/lógica; cabecera con cifras reales, pasos 1-2-3, chips de zona que espejean el
     <select>, leyenda, tabla y tarjetas móviles con relieve.
   - Reservar: 3 pasos (Ruta / Tus datos / Pago) sobre el wizard existente, validación por paso, Enter no
     envía antes del paso 3, resumen oscuro lateral (barra fija en móvil) con total animado; confirmación y
     letrero de pickup en rejilla nueva. Pagos siguen simulados.
   - Zonas: mapa ilustrado (SVG propio, no a escala) de la costa este con toggle PUJ/SDQ, rutas animadas,
     nodos por zona con tamaño = hoteles en el tarifario, panel con conteo, "desde" y hoteles reales, botón
     que filtra el tarifario por esa zona y aeropuerto; franja de tiempos de viaje.
   - Destinos: stack sticky + modal SIN cambios de comportamiento (pin, autoplay, sin flechas, hoja móvil,
     bloqueo de scroll, Escape). Nuevo: cabecera con índice, numeración grande, contador flotante con
     progreso, fondo que oscurece a "atardecer" durante el stack, CTA de cierre y, en el modal,
     "Reservar esta ruta / Ver tarifas" (cierra el modal y lleva a la sección).
   - FAQ: dos columnas, acordeón con grid-rows, ayuda por WhatsApp.
   - Cierre + footer: escena de atardecer con mar animado, palmera y espuma; titular final, CTAs magnéticos,
     boleto interactivo (inclinación con el puntero), navegación eco, contacto en tarjetas, legales,
     wordmark "Karún" gigante.
   - Separadores: sistema de olas/espuma/duna (.k23-sep) con trazo de espuma animado.

4) Correcciones de contenido
   - Galerías del modal de Destinos: se retiraron fotos que no eran de RD (SDQ mostraba Machu Picchu, Oporto,
     Chichén Itzá y Madrid; PUJ un buzo y los Dolomitas). Ahora solo imágenes del proyecto (image-slots).
   - FAQ alineada con el tarifario: "por vehículo", capacidades por clase (ya no "van de 8 pasajeros"),
     pago con tarjeta/PayPal/efectivo, equipaje voluminoso → techo alto.
   - Idiomas: cualquier idioma distinto de ES muestra el copy EN de los bloques bilingües (antes RU/ZH/NL/PL
     caían al español). ES/EN completos; FR/DE/PT/IT/RU/ZH/NL/PL no se rompen.

5) Técnica
   - Nuevos: assets/karun-v23.css (capa visual), assets/karun-v23.js (motion/interacción).
   - Sin GSAP ni Lenis: un solo loop rAF de scroll nativo. Motivo: el stack sticky de Destinos y el bloqueo
     de scroll del modal dependen del scroll nativo; el motion propio cubre todo sin 100 KB extra.
   - Remount-safe: nada reescribe markup de React; estado en atributos de <html>, variables CSS o
     contenedores propios (chips, nodos del mapa, CTA del modal); MutationObserver re-vincula.
   - prefers-reduced-motion: sin canvases, sin scroll-scrub (llegada/servicios en flujo normal), sin
     animaciones infinitas (verificado: 0 animaciones infinitas activas).
   - karun-wizard.js, support.js, image-slot.js, tarifario.json/.data.js y tools/ NO se modificaron.

6) Pruebas (Chromium headless, React/Babel servidos localmente)
   - 1440×900: wizard paso 1 (bloquea sin hotel) → 2 (bloquea sin nombre) → 3 → confirmar → letrero →
     editar; W Punta Cana / Starex / 2 pax = US$50 (coincide con la hoja). Cotizar desde flota (Cadillac) y
     desde fila del tarifario; chips de zona; mapa SDQ → Santo Domingo (14 hoteles, desde US$60) → tarifario
     filtrado SDQ/santo-domingo. Modal de Destinos: abre, bloquea scroll, CTA cierra y navega.
     ES → EN → DE → ES sin textos cruzados; sin scroll horizontal; 0 errores de consola.
   - 390×844 móvil: drawer, capítulos apilados, tarjetas de tarifas, resumen fijo, mapa; sin desbordes.
   - Nota: el MP4 del hero (H.264) no reproduce en Chromium open-source; se ve el póster. En Chrome/Safari
     reales se reproduce.

7) Pendiente de confirmar con el cliente (se mantiene de v22)
   - Whala Urban (Suburban 45/55 vs 50/60 de sus vecinos), Royalton Bavaro (Starex 40/50 vs 25/35 de su zona),
     Solati → columna Hiace / Techo alto, regreso = 2 × una vía (el tarifario no trae descuento ida y vuelta).
   - Datos placeholder: WhatsApp +1 809 000 0000, hola@losyeyes.do, licencia #RD-XXXX, fotos referenciales
     de flota. Idiomas de atención FR/DE (heredado de v22) — confirmar.

---
Beach / sensory redesign + tarifario refresh (v22) — 2026-09-21 (America/Mexico_City)

1) Tarifario oficial (PUJ + SDQ, 4 clases de vehículo)
   - tools/build_tarifario.py lee las 4 hojas visibles del .xlsm (DESDE PUNTA CANA SUB CADILLAC,
     DESDE AEROP PUNTA CANA MINIVAN, DESDE AEROP SDQ, DESDE AEROP SDQ SUB CAD; las hojas antiguas
     ocultas se ignoran) y genera assets/tarifario.json (legible) + assets/tarifario.data.js
     (mismo dato, carga sin fetch; funciona también desde file://).
   - Cada precio fue verificado contra los 5 PDFs enviados, fila por fila: 0 diferencias.
     PUJ = 115 hoteles / 10 zonas · SDQ = 134 hoteles / 13 zonas. PUJ Starex/Techo alto coincide
     exactamente con el tarifario anterior.
   - Clases: starex (1–6) · hiace / techo alto (7–10) · suburban (1–4) · cadillac (1–4).
     "Hiace Toyota" y "Techo Alto" son la misma columna (los dos PDFs tienen precios idénticos).
   - "Pax adicional por vía" se guarda por clase (Playa Palmera: 5 en Starex/Hiace, 6 en Suburban/Cadillac).
   - Normalización sin tocar precios (meta.normalizations): Occiental→Occidental, Couryard→Courtyard,
     numeración de lista "1 EMBASSY…" eliminada; Hard Rock duplicado en SDQ descartado (meta.duplicatesDropped).
   - Un hotel solo existe donde su hoja lo publica; no se inventó ninguno.
   - Para confirmar con el cliente (datos tal cual vienen): Whala Urban (Suburban 45/55 vs 50/60 de sus vecinos),
     Royalton Bavaro (Starex 40/50 vs 25/35 de su zona), Solati mapeada a la columna Hiace / Techo alto.

2) Wizard y buscador (karun-wizard.js reescrito, remount-safe)
   - Por tramo: aeropuerto (PUJ/SDQ) · sentido (llegada/salida) · hotel agrupado por zona del aeropuerto ·
     vehículo (Automático = clase más barata que cubre el grupo, o clase fija) · fecha/hora/pax/vuelo.
   - Precio: tarifa base cubre hasta paxMax de la clase; cada pax adicional cobra el "extra" de esa hoja.
     Sugerencia si otra clase sale más barata. Tramos extra = una vía más a la tarifa publicada
     (demo; las hojas no traen descuento de ida y vuelta). El tramo de regreso se pre-llena.
   - Buscador: origen, clase de vehículo, texto sin acentos, zona; tarjetas en móvil.
   - Tarjetas de flota con "Desde US$ … PUJ / SDQ" calculado del dato; pax corregidos según hojas
     (Suburban 1–4, Cadillac 1–4). Mock de pago, letrero de pickup y localStorage intactos
     (formato de reserva guardada v2; los borradores viejos se ignoran).

3) Diseño playero / sensorial
   - Tipografía: Fraunces (titulares, eje SOFT) + Figtree (texto/UI/precios), woff2 propios en assets/fonts
     (OFL, ver assets/fonts/OFL.txt). Se eliminó Google Fonts. Ñ, tildes, ¡ ¿ verificados.
   - assets/karun-beach.css: sol suave + mar de respaldo + orilla animada en el hero, divisores de ola,
     lavados de luz por sección, relieve/insets, brillo en botones, barra de progreso de scroll (CSS
     scroll-driven, con fallback JS), parallax del hero. Paleta teal sin cambios.
   - assets/karun-fx.js: destellos de sol y partículas en el hero (canvas, pausa fuera de vista),
     reflejo/parallax en tarjetas de flota. Todo apagado con prefers-reduced-motion.
   - Destinos (stack sticky + modal, autoplay, sin flechas, scroll lock, Escape) sin cambios de comportamiento.
   - Sin fotos/video nuevos en esta pasada (la sandbox no puede descargar de Unsplash/Pexels).

4) Pruebas realizadas (Chromium headless)
   - 21 casos de precio contra valores de los PDFs + 996 combinaciones hotel×clase contra el JSON.
   - Flujo: buscar → reservar desde fila → tramo de regreso → confirmar → letrero PNG → editar; ES↔EN.
   - Destinos: stack sticky, modal abre/cierra con Escape; móvil 390px sin overflow; reduced-motion sin animaciones infinitas.

---
Destinos detail modal scroll lock — 2026-09-21 (America/Mexico_City)
- Fixed-position body lock now preserves and restores the exact page scroll position.
- Backdrop/document touch and wheel gestures are blocked while the modal body keeps contained scrolling.
- Added overscroll containment in both modal style blocks; autoplay and remount teardown remain unchanged.

---
Transportes Los Yeyes — index.html patch notes
Date: 2026-09-11 (America/Mexico_City)

1) Cookie consent persistence
   - localStorage key ly_cookie_consent stores {v:'accepted'|'rejected', exp}
   - 12-month expiry (365 days from accept/reject)
   - Initial state reads consent via IIFE; readCookieConsent() / writeCookieConsent(value) helpers
   - Banner stays hidden when a valid choice exists; footer Cookie preferences still opens the modal
   - Modal accept/reject also persists

2) Mobile navbar (≤900px)
   - .nav-toggle hamburger (desktop hidden); .nav-drawer overlay + panel with same links
   - State: navOpen; renderVals: toggleNav, closeNav, navDrawerDisplay
   - Closes on link click, Escape, and outside click
   - Lang switcher + Book remain in the top bar on mobile

3) Destinations carousel
   - Replaced #expPin sticky/scroll-timeline (420vh) with .dest-rail horizontal scroller
   - Scroll-snap, touch-native scroll, optional scrollbar hide
   - .dest-prev / .dest-next arrow buttons; destScroll(dir) + initDestCarousel() (pointer drag on mouse only)
   - EN/ES/FR/DE/PT/IT d_sub copy updated for swipe/arrows

4) Decorative line-art (.ly-art)
   - Palms, waves, sun arcs, dashed routes in #services, #van, #prices, #map, timeline, #faq
   - Teal/coral/sun strokes, low opacity, pointer-events:none, aria-hidden
   - @keyframes ly-float; prefers-reduced-motion disables animation

5) Selection / drag guards
   - .lm user-select:none; form fields re-enabled
   - img / image-slot / video / svg user-drag:none
   - componentDidMount sets draggable=false and blocks dragstart on media (not form fields)

Sanity: kickHeroVideo, readCookieConsent, dest-rail, nav-toggle, ly-art, user-select present.
DCLogic class closing brace present; node --check of script passes.
Only index.html edited (+ this PATCH_NOTES.txt).

---
Karún Travel Group rebrand — patch notes
Date: 2026-09-17 (America/Mexico_City)

1) Full rebrand
   - "Transportes Los Yeyes" / "Los Yeyes" -> "Karún Travel Group" across
     title, meta, nav, footer, all 10 language blocks in the main app script.
   - Logo assets (favicons, nav mark, apple-touch-icon) regenerated from the
     client-provided Karún PNG; old Los Yeyes files overwritten.
   - Palette derived from the logo: --teal -> navy #151a54 / #0a0d33,
     --coral -> red #c8102e (CSS custom properties on .lm, so all .btn/.chip/
     map-pin/etc. that reference var(--teal)/var(--coral) recolored globally).
     Every hardcoded legacy hex that bypassed the variables was also swapped.

2) Real tarifario
   - assets/tarifario.json: all 115 hotels / 10 zones extracted verbatim from
     the client's PDF (Tarifario_regular_Karun_Travel__Definitivo_MINIVAN__
     TECHO_ALTO.pdf). Mirrored inline in karun-wizard.js for zero-fetch
     reliability (works from file:// too).

3) New "Interactive demo" section — Fleet, rate search, booking wizard
   - Added karun-wizard.js (self-contained, no build step) + matching CSS in
     index.html (krn-* classes) implementing:
     - Fleet cards for the 5 requested vehicles (illustrative SVG placeholders
       — see assets/CREDITS.md — real photos pending).
     - Searchable/filterable rate table over all 115 hotels.
     - Multi-leg booking wizard: add/remove legs, per-leg pax-band pricing
       (Starex 1-6 / Techo Alto 7-10 / +extra-pax beyond 10), vehicle picker,
       passenger fields, mock payment (card/PayPal/cash — no real charge),
       booking-code confirmation, canvas-drawn downloadable pickup sign,
       copy-summary, edit-booking, localStorage draft persistence.
   - IMPORTANT ARCHITECTURAL NOTE: this section is NOT inside the <x-dc>
     template that the main landing's React runtime compiles and re-renders.
     Verified via Playwright that placing new dynamic content inside that
     tree gets wiped on every re-render (React reconciles children strictly
     from the compiled template), and that plain `onclick="..."` attribute
     strings are passed through as literal props (not wrapped into real
     functions), so they silently fail to fire once mounted by that runtime.
     The whole demo section was therefore moved to sit as an independent
     sibling block right after `</x-dc>` in the DOM, with its own `.lm`-class
     wrapper (reusing the same CSS custom properties/utility classes) and its
     own ES/EN toggle (default ES), so it works identically regardless of
     whether the React bundle loads from unpkg.com.
   - Pickup-sign canvas draws the logo from an embedded base64 data URI
     (not assets/logo-mark.png) specifically to avoid canvas tainting under
     file:// previews; confirmed with Playwright that toDataURL()/toBlob()
     both work after this change.

4) Sanity checks performed
   - Whole-file HTML tag-balance check (zero mismatches).
   - node --check on karun-wizard.js.
   - Playwright (real Chromium) end-to-end: fleet quote CTA, hotel search/
     filter, multi-leg pricing math, form submit -> confirmation -> pickup
     sign download -> copy-summary -> edit-booking -> language toggle.
   - Confirmed the pre-existing unpkg.com React-load-over-file:// failure
     also occurs on the untouched original index.html — not a regression.

Not done in this pass: full translation of the new Fleet/demo copy into the
8 non-ES/EN languages already in the site (fr/de/pt/it/ru/zh/nl/pl); the new
section only offers ES/EN. Real fleet photography still pending from the
client.


---
Merge into los-yeyes Pages repo — patch notes
Date: 2026-09-17 (America/Mexico_City)

1) Merged Claude Karún delivery into /workspace/los-yeyes-pages
   - Took Claude index.html + karun-wizard.js as feature base.
   - Preserved support.js, image-slot.js, media/hero-starex-loop.mp4,
     assets/rd-map.png from the existing Pages repo.
   - Added assets/tarifario.json (115 hotels), Karún logo/favicon set,
     README.md, CREDITS.md.

2) Fleet photos
   - Created assets/fleet/ with five referential JPEGs:
     starex.jpg, escalade-2020.jpg, solati-2014.jpg, hiace-2019.jpg,
     suburban-lwb-2020.jpg (Wikimedia Commons + Pexels; see CREDITS.md).
   - Replaced SVG placeholders in #fleet cards with <img> tags; CSS
     object-fit:cover on .krn-fleet-media img.

3) Logo cleanup
   - Renamed unused logo-los-yeyes.png/.jpg → logo-karun.png/.jpg
     (content already Karún; filenames now match brand).

4) i18n
   - html lang=es; React default lang es; demo scope data-lang=es.
   - ES+EN strings retained; Spanish is the default visible UI.

5) Static-only constraints unchanged
   - Mock payment / no WhatsApp API / no AI mention on public site.

---
2026-09-17 — Ocean palette + section order (America/Mexico_City)

- Restored beach/sea chrome: sand #f3efe4, sea teal #0e8fa0 / #0a6b7c, sunset coral #f2864b
  (navy/red UI chrome removed; Karún logo assets unchanged).
- Nav (desktop + hamburger): Servicios → Flota → Tarifas → Reservar → Zonas → Destinos → FAQ
  with Tarifas → #rates-search and Reservar → #reservar.
- Slimmed #van and #prices inside React landing to short teasers + CTAs to #fleet / #rates-search.
- Softened #demo-start copy (“Reserva y flota”) and added a thin sea-wave divider above the
  post-footer demo scope (demo nodes remain outside <x-dc>).
- Added “Reservar demo” → #reservar on hero, book, services, and final CTA.

---
2026-09-17 — True scroll order + collapsed tarifario (America/Mexico_City)

1) Scroll order matches navbar
   - Moved #fleet, #rates-search, #reservar into <x-dc> as static template HTML
     immediately after #services (React preserves them; no post-footer demo block).
   - Removed #van / #prices teasers and the old #krn-demo-scope section markup.
   - DOM/nav sequence: #services → #fleet → #rates-search → #reservar → #map →
     #destinations → #faq (hero/#book stay above; timeline/reviews/CTA/footer after).

2) karun-wizard.js remount-safe
   - Event delegation via data-krn-quote / data-krn-action (no inline onclick in template).
   - Idempotent mount + MutationObserver on x-dc; re-paints when React remounts wipe
     dynamic nodes (data-krn-ready / data-krn-painted markers).
   - Language follows the main React .lm[data-lang]; banner wrap stays in sync.

3) Tarifario UX
   - Initially shows 12 rows; “Ver más tarifas” / “Show more rates” expands the rest
     (with “Ver menos”); search/zone filter shows all matches automatically.
   - Indicative / demo messaging retained on the section and count line.

Ocean palette unchanged. Smoke: node --check karun-wizard.js; section ids unique.

---
2026-09-21 — Destinations sticky stack (America/Mexico_City)

- Replaced #destinations horizontal .dest-rail carousel + arrow buttons with a vertical
  sticky stack (#dest-stack / .dest-card), matching the Noook portfolio pin pattern.
- Ocean palette (ink veil, sand section, teal chips); JS sets --in / --cover for pin scale
  and cover fade. Remount-safe initDestStack; reduced-motion / short viewport → plain list.
- i18n d_sub (all langs): destinations stack as you scroll (no swipe/arrow copy).

---
2026-09-21 — Destinos detail modal (America/Mexico_City)

1) Floating destination modal on sticky Destinos cards
   - Click / Enter / Space on `.dest-card[data-dest]` opens overlay + centered dialog
   - Image carousel (prev/next + dots), gentle autoplay paused on hover/focus;
     `prefers-reduced-motion` disables autoplay and slide transition
   - Close: X, backdrop, Escape; body scroll lock; focus restored to the card
   - Mobile near full-bleed; ocean palette (teal / sand / ink)
   - Cards: cursor pointer + “Ver más / See more” affordance chip (sticky stack
     `--in` / `--cover` / IntersectionObserver unchanged)

2) Remount-safe JS (same pattern as initDestStack)
   - `initDestModal` / `wireDestModalHost`; host lives on `document.body` outside
     React remounts; stack listeners teardown + rebind in componentDidUpdate

3) Content & assets
   - 5 destinations: puj, bavaro, cap-cana, saona, santo-domingo
   - Tourist-transfer copy (3 paragraphs each) in all 10 site languages via
     `destModalCopy`; chrome keys `dm_close` / `dm_prev` / `dm_next` / `dm_gallery`
     in every i18n block
   - Local images: `assets/destinations/<slug>/01–04.jpg` (Unsplash; CREDITS.md)

No fake prices or testimonials in the modal. Sticky stack scroll still works.

---
2026-09-21 — Destinos modal polish: autoplay + mobile sheet (America/Mexico_City)

1) Dynamic carousel autoplay
   - setTimeout chain ~3.8s (was setInterval 5.2s); resets on prev/next/dot
   - Visible bottom progress bar + expanding active pill-dot fill; pause CSS with hover/focus/touch
   - Subtle Ken Burns on active slide; prefers-reduced-motion disables autoplay + motion
   - Smooth transform track (.55s); autoplay still runs on mobile when modal is open

2) Mobile modal (≤640px) — floating sheet, not full-bleed
   - Backdrop keeps safe-area padding (~10–14px); dialog max-height ~90dvh, rounded 22px
   - Flex column: carousel fixed aspect (shrink 0); body scrolls inside dialog
   - Both CSS copies (head + x-dc) kept in sync; desktop modal unchanged in spirit

Sticky dest-stack / i18n untouched.

---
2026-09-21 — Carousel controls + centered mobile modal (America/Mexico_City)

- Removed prev/next buttons, ArrowLeft/ArrowRight handlers, and related ARIA/i18n; dots, autoplay, progress, and Ken Burns remain.
- Centered the ≤640px rounded modal with safe-area padding, ~90dvh cap, and internal body scrolling in both CSS copies.
